extends ../layout

block content
  .container
    h1.page-header 회원 목록
    
    table.table.table-striped.table-bordered
      thead
        tr
          th 이름
          th 이메일
          th 가입일시

      tbody
        each user in users
          tr
            td
              a(href='/users/#{user._id}')= user.name
            td= user.email
            td= moment(user.createdAt).format('YYYY-MM-DD HH:mm:ss')

    a.btn.btn-primary(href='/users/newadmin') 사용자추가
